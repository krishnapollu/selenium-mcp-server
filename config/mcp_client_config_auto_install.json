{
  "mcpServers": {
    "selenium": {
      "command": "powershell",
      "args": ["-Command", "pip install --user selenium-mcp-server --quiet > $null 2>&1; python -m selenium_mcp_server"]
    }
  }
} 